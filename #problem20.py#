#based on moving the 10's digit up the line
n = {}
n_prime = {}

for i in range(0, 160):
    n[i] = 0
    n_prime[i] = 0
    
n[0] = 1 #start with 2^1

for t in range(2, 101): #multiply by 99 more 1s
    
    for i in range(0,160):
        temp = n[i]*t
        div_pow = 0
        while temp > 0:
            


    for i in range(0, 160):
        n[i] = n_prime[i]

sum = 0
for i in range(0, 160):
    sum += n[i]

print "final sum: ", sum
